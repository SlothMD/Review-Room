# Backup And Sync

The repository source and `refs/` project memory should be committed to git. Do not commit local secrets, credentials, machine-specific tokens, generated review history, or private reviewer notes.

Chrome sync may store extension guidance and selected model via `chrome.storage.sync`; this is runtime user configuration, not repository state.
